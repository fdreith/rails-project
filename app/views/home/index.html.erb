<h2><%= current_user.name != nil ? current_user.name : current_user.email %> Homepage </h2>

  <h3>My Book Clubs</h3>
<% if !current_user.book_clubs.empty?%>
  <ul>
  <% current_user.book_clubs.each do |book_club| %>
  <li><%= book_club.name %></li>
  <%end%>
  </ul>
<%else%>
  You have no current Book Clubs. 
<%end%>

<h3>My Read Books </h3>
<% if !current_user.books.empty?%>
  <ul>
  <% current_user.books.each do |book| %>
  <li><%= book.title %> by <%= book.author.name %> - <%= book.genre.name %> - <%= book.page_count %> pages</li>
  <%end%>
  </ul>
<%else%>
  You have no read books. 
<%end%>

<%# <%= button_to "New Book", { :controller => "books", :action => "new" }, :method => :get %> 
<%# <%= link_to '<button type="New Book">New Book</button>'.html_safe, new_book_path %> 
<%= button_to 'Create new Book', new_book_path, method: :get %>
<%= button_to 'Create new Book Club', new_book_club_path, method: :get %>
<%= button_to 'Browse Books', books_path, method: :get %>